export interface ServiceDetailResponse {
  serviceDetailID: number;
  code: string;
  minRange: number;
  maxRange: number;
  price: number;
  extraPricePerMM: number;
  status: string;
  serviceID: number;
}
